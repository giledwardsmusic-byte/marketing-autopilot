name: Deploy and verify

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: marketing-autopilot-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      MA_BASE_URL: https://marketing-autopilot.giledwardsmusic.workers.dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Verify release manifest
        run: npm run verify:release
      - name: Detect deploy credentials
        id: cf
        shell: bash
        run: |
          if [ -n "$CLOUDFLARE_API_TOKEN" ] && [ -n "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "Cloudflare deployment is blocked: CLOUDFLARE_API_TOKEN and/or CLOUDFLARE_ACCOUNT_ID is not configured in GitHub Actions secrets." >&2
          fi
      - name: Block false-green deployment
        if: steps.cf.outputs.ready != 'true'
        shell: bash
        run: |
          echo "Deployment did not run, so this workflow must not report success." >&2
          echo "Add CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID as GitHub Actions secrets, then rerun this workflow." >&2
          exit 1
      - name: Deploy Worker
        if: steps.cf.outputs.ready == 'true'
        run: npx wrangler deploy
      - name: Verify live release
        if: steps.cf.outputs.ready == 'true'
        shell: bash
        run: |
          # Cloudflare can take a few seconds to make the new Worker globally visible.
          for attempt in 1 2 3 4 5 6; do
            if npm run probe:live; then
              exit 0
            fi
            if [ "$attempt" -lt 6 ]; then sleep 10; fi
          done
          echo "Deployment completed, but the expected live release did not become visible in time." >&2
          exit 1
      - name: Report live Drive ingestion status
        if: steps.cf.outputs.ready == 'true'
        run: node scripts/probe-drive-status.mjs
